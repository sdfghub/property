# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Builds
dist/
build/
coverage/
*.tsbuildinfo

# Environment / secrets
.env
.env.local
.env.*.local
/github
/github-token

# Prisma
prisma/dev.db
prisma/*.sqlite

# Logs
logs/
*.log
*.log.*

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Docker / containers
# NOTE: .dockerignore IS tracked — the deploy images build with the repo root as context,
# so it is part of the build contract. Only a developer's own compose override is ignored.
docker-compose.override.y*ml
*.tar

# GitHub artifacts
.github/*
!.github/workflows/
!.github/workflows/**

# CDK / Cloud
infra/cdk/cdk.out/
infra/cdk/dist/
*.pem

# Archives / temporary
*.zip
*.tgz
*.tmp
property-expenses-59672-firebase-adminsdk-fbsvc-4aee2fc092.json

# Captured script output (e.g. `npm run db:flush > debug`) — never source
debug
